package model;

public class HumanPlayer extends Player {

    //no need to have anything specific-same as that of player

    public HumanPlayer(int id, String name,Symbol symbol,PlayerType playerType){
        super(id,name,symbol,playerType);
    }
}
